import { CATALOG_PLANS } from "@/lib/catalog";
import type { Plan } from "@/lib/types";

/** Fake catalogue — CCP MainPlanPrepaid_Kini until a live API is connected. */
export const MOCK_PLANS: Plan[] = CATALOG_PLANS;
