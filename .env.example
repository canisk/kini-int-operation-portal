# Paste your real plans API base URL here (no trailing slash).
# When set, src/lib/plans-api.ts uses the REAL API blocks instead of the fake ones.
# Example: https://api.telecom.internal/api/v1
PLANS_API_BASE_URL=

# Optional auth token for the real API (uncomment usage in plans-api.ts)
# PLANS_API_TOKEN=
